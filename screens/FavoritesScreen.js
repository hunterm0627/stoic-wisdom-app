import React from 'react';
import { Text, View } from 'react-native';

const FavoritesScreen = () => {
    // Here you would typically fetch the list of favorite quotes from your state
    // and render them. For now, we'll just render a simple message.

    return (
        <View>
            <Text>Favorites Screen</Text>
        </View>
    );
};

export default FavoritesScreen;